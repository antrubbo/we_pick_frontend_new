const Account = () => {
    // useEffect send request for user info - ("/current-user")
    // req made with JWT token
    // if no JWT token/invalid, redirect to login page

    return(
        <div id="account">
            <h1>ACCOUNT page</h1>
        </div>
    )
}

export default Account